import React, { useState } from "react";
import Button from "./components/button"; // Make sure the path to the Button component is correct

function App() {
useState()

  return (
    <div>
      <Alert>My alert</Alert>
      <Button color="primary" onClick={() => console.log('clicked')}>My Button</Button>
    </div>
  );
}

export default App;
